server:
    port: 80

spring:
  application:
    name: zulu-service
  zipkin:
      baseurl: http://zipkin-server
  sleuth:
    sampler:
      percentage: 1

eureka:
    client:
        serviceUrl:
            defaultZone: ${vcap.services.eureka-service.credentials.uri:http://eureka-server}/eureka/